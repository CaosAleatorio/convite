import Envelope from './components/Envelope';

export const metadata = {
  title: 'Convite Especial',
  description: 'Você foi convidado para um evento especial!',
};

export default function Home() {
  return <Envelope />;
}
